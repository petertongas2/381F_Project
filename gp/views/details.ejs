<html>
<head>
    <title>演唱會詳情 - 票務系統</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>演唱會詳情</h1>
        </div>
        <div class="concert-details">
            <h2><%= concert.title %></h2>
            <div class="info-group">
                <strong>日期：</strong><%= concert.date %>
            </div>
            <div class="info-group">
                <strong>時間：</strong><%= concert.time %>
            </div>
            <div class="info-group">
                <strong>地點：</strong><%= concert.location %>
            </div>
            <div class="info-group">
                <strong>描述：</strong>
                <p><%= concert.description %></p>
            </div>
            <div class="info-group">
                <strong>內容：</strong>
                <p><%= concert.content %></p>
            </div>
            <div class="info-group">
                <strong>藝人：</strong><%= concert.artist %>
            </div>
            <div class="info-group">
                <strong>票價：</strong><%= concert.ticketFee %>
            </div>
            <div class="action-buttons">
                <a href="/edit?_id=<%= concert._id %>" class="btn">編輯</a>
                <a href="/delete?_id=<%= concert._id %>" onclick="return confirm('確定要刪除此演唱會嗎？')" class="btn btn-danger">刪除</a>
                <a href="/content" class="btn">返回列表</a>
                <a href="/logout" class="btn btn-danger">登出</a>
            </div>
        </div>
    </div>
</body>
</html>
